== Sample MANAstats client

This application exercises OAuth access to manastats.org as well as the access API.

== Getting Started

Install this app and launch in the usual rails way: 
  bundle install
	rake db:create
	rake db:migrate
	rails server
	
Look at home_controller.rb to how things things work.

== API

All values are returned as json

/api/forms => returns a list of the forms in the midwive's log